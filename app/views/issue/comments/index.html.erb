<div class="dsc-Comment_List" id="js-issue-comments" data-channel="issue-comments" data-issue-id="<%= current_issue.id %>">
  <% @comments.each do |comment| -%>
    <%= render partial: 'unknown_comment', locals: { comment: comment } %>
  <% end -%>
</div>
