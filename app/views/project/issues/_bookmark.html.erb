<% if issue.bookmarked? -%>
  <%= render partial: 'project/issues/bookmarked', locals: { issue: issue } %>
<% else -%>
  <%= render partial: 'project/issues/unbookmarked', locals: { issue: issue } %>
<% end -%>
