<% if @issue.closed? -%>
  <% breadcrumb :show_closed_issue, current_project, @issue %>
  <%= render 'closed' %>
<% else -%>
  <% breadcrumb :show_issue, current_project, @issue %>
  <%= render 'opened' %>
<% end -%>
